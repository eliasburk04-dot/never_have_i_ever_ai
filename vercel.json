{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "[ -d .flutter ] || git clone https://github.com/flutter/flutter.git --depth 1 -b stable .flutter; ./.flutter/bin/flutter config --no-analytics; ./.flutter/bin/flutter --version; ./.flutter/bin/flutter pub get",
  "buildCommand": "./.flutter/bin/flutter build web --release --dart-define=SUPABASE_URL=${SUPABASE_URL} --dart-define=SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
