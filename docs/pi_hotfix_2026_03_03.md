# Pi Hotfix 2026-03-03 (Apple 4.3(b))

## Scope

Hotfix deployed directly on Pi (`milkathedog@100.69.69.19`) because `/opt/neverhavei` is the production source and not a git repo.

Main goals:

1. Add Creator Pack + Custom Question support to active game module.
2. Persist new fields in DB (`pack_ids`, `custom_questions`).
3. Expose `pack_ids` + `custom_questions_count` in lobby state.
4. Remove drinking/alcohol wording from question pool and validation.

## Pi Source-of-Truth Files Changed

Active code path:

1. `/opt/neverhavei/api/src/games/never_have_i_ever/engine.ts`
2. `/opt/neverhavei/api/src/games/never_have_i_ever/routes.ts`
3. `/opt/neverhavei/api/src/games/never_have_i_ever/selectors.ts`
4. `/opt/neverhavei/api/src/game/safety.ts`
5. `/opt/neverhavei/migrations/004_creator_packs.sql`

Backups created before patching:

1. `/opt/neverhavei/backups/hotfix_2026-03-03_43b/`
2. `/opt/neverhavei/backups/hotfix_2026-03-03_43b/active_before_patch/{engine.ts,routes.ts,selectors.ts}`

## Exact Diffs (Key Hunks)

### `engine.ts`

```diff
+const CreatorPackIdSchema = z.enum(['icebreakers', 'deep_talk', 'date_night']);
+...
+packIds: z.array(CreatorPackIdSchema).max(6).optional().default([]),
+customQuestions: z.array(z.string().trim().min(1).max(180)).max(MAX_CUSTOM_QUESTIONS).optional().default([]),
...
+pack_ids?: string[];
+custom_questions?: unknown;
...
+const selectedPackIds = normalizePackIds(lobby.pack_ids);
+const effectiveCategories = mergeCategoryFilters(lobby.categories, selectedPackIds);
+const customCandidates = customQuestionCandidates(lobby, usedIds).filter(
+  (q) => !recentSignals.recentIds.includes(q.id),
+);
...
+if (shouldUseCustomQuestion) {
+  const pickedCustom = weightedPick(customCandidates, sessionSeed ^ 0x4f4f4f4f, opts.nextRoundNumber, customWeights);
+  questionText = pickedCustom.text;
+  questionSourceId = pickedCustom.id;
+}
...
-l.nsfw_enabled, l.boldness_score, l.current_tone, l.escalation_history, l.used_question_ids, l.game_key
+l.nsfw_enabled, l.boldness_score, l.current_tone, l.escalation_history, l.used_question_ids,
+l.categories, l.pack_ids, l.custom_questions, l.game_key
```

### `routes.ts`

```diff
-INSERT INTO lobbies (game_key, code, host_id, language, max_rounds, nsfw_enabled, categories)
-VALUES ($1,$2,$3,$4,$5,$6,$7)
+INSERT INTO lobbies (game_key, code, host_id, language, max_rounds, nsfw_enabled, categories, pack_ids, custom_questions)
+VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9::jsonb)
...
+body.packIds ?? [],
+JSON.stringify(body.customQuestions ?? []),
...
+categories: Array.isArray(lobby.categories) ? lobby.categories : [],
+pack_ids: Array.isArray(lobby.pack_ids) ? lobby.pack_ids : [],
+custom_questions: Array.isArray(lobby.custom_questions) ? lobby.custom_questions : [],
```

### `selectors.ts`

```diff
+const packIds = Array.isArray(lobby.pack_ids) ? lobby.pack_ids.map((value: unknown) => String(value)) : [];
+const customQuestions = Array.isArray(lobby.custom_questions)
+  ? lobby.custom_questions.map((value: unknown) => String(value ?? '').trim()).filter(Boolean)
+  : [];
+const { custom_questions: _customQuestions, ...lobbyRest } = lobby;
+const lobbyPayload = {
+  ...lobbyRest,
+  pack_ids: packIds,
+  custom_questions_count: customQuestions.length,
+};
...
-return { lobby, players, round, answers, answered };
+return { lobby: lobbyPayload, players, round, answers, answered };
```

### `safety.ts`

```diff
-/\b(trinkspiel|trinken|alkohol|bier|wodka|tequila|whisky|rum|kater)\b/i,
+/\b(trinkspiel|trinken|alkohol|bier|wodka|tequila|whisky|kater)\b/i,
```

### Migration

```sql
ALTER TABLE lobbies
  ADD COLUMN IF NOT EXISTS pack_ids TEXT[] DEFAULT '{}';

ALTER TABLE lobbies
  ADD COLUMN IF NOT EXISTS custom_questions JSONB DEFAULT '[]'::jsonb;
```

## Exact Deploy Commands (Executed)

```bash
# backup
ssh milkathedog@100.69.69.19 'mkdir -p /opt/neverhavei/backups/hotfix_2026-03-03_43b/active_before_patch && cp /opt/neverhavei/api/src/games/never_have_i_ever/engine.ts /opt/neverhavei/backups/hotfix_2026-03-03_43b/active_before_patch/engine.ts && cp /opt/neverhavei/api/src/games/never_have_i_ever/routes.ts /opt/neverhavei/backups/hotfix_2026-03-03_43b/active_before_patch/routes.ts && cp /opt/neverhavei/api/src/games/never_have_i_ever/selectors.ts /opt/neverhavei/backups/hotfix_2026-03-03_43b/active_before_patch/selectors.ts'

# copy patched files
scp backend/src/games/never_have_i_ever/engine.ts milkathedog@100.69.69.19:/opt/neverhavei/api/src/games/never_have_i_ever/engine.ts
scp backend/src/games/never_have_i_ever/routes.ts milkathedog@100.69.69.19:/opt/neverhavei/api/src/games/never_have_i_ever/routes.ts
scp backend/src/games/never_have_i_ever/selectors.ts milkathedog@100.69.69.19:/opt/neverhavei/api/src/games/never_have_i_ever/selectors.ts

# apply migration
ssh milkathedog@100.69.69.19 "cd /opt/neverhavei && sudo docker compose exec -T db psql -U postgres -d neverhavei -f /migrations/004_creator_packs.sql"

# rebuild api
ssh milkathedog@100.69.69.19 "cd /opt/neverhavei && sudo docker compose up -d --build api"
```

## Runtime Verification (Executed on 2026-03-03)

1. Health:

```bash
curl -i http://127.0.0.1:3000/health
# HTTP/1.1 200 OK
# {"ok":true,"db":true}
```

2. Container state:

```bash
cd /opt/neverhavei && sudo docker compose ps
# api: Up (... healthy)
# db:  Up (... healthy)
```

3. API payload check (`/lobby/create` + `/lobby/:code/state`):

```json
{
  "create": {
    "pack_ids": ["icebreakers"],
    "custom_questions": ["Never have I ever changed plans at the last minute"]
  },
  "state": {
    "pack_ids": ["icebreakers"],
    "custom_questions_count": 1
  }
}
```

4. Round selection check (`/lobby/:code/start` with 2 players):
first round used custom source id (`question_source_id` starts with `custom:`), confirming selection consumes custom questions.
